# Copy to .env for local `astro dev`/`astro build` use.
# For local `wrangler dev`/`wrangler pages dev` runtime bindings, use
# .dev.vars instead (see .dev.vars.example).
LASTFM_API_KEY=changeme
LASTFM_USERNAME=patcartelli
TADB_API_KEY=changeme
